import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'apiRTC-ionic',
  webDir: 'dist',
  //server: {
  //  hostname: 'localhost',
    //iosScheme: "file",
    //androidScheme: "https",
  //},
};

export default config;
